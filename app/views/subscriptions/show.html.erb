    <div class="exit-btn"><%= link_to "X", dashboard_path %>
    </div>

<%= link_to @subscription.url do%>
  <div class="card-img">
    <img src=<%= @subscription.image_url %>>
    </div>
    <div class="card-description">
    <h1><%= @subscription.offer.service_name %></h1>
    <h4><%= @subscription.offer.category.name %></h4>
    </div>
      <p>edit</p>
<% end %>
      <div class="card-body">
        <%= render 'shared/card-details',
            icon: "💳",
            type: "Offer",
            data: @subscription.offer.name
        %>
        <br>
        <%= render 'shared/card-remindme' %>
      </div>

    <div class="card-buttons-wrapper">
      <div class="card-buttons">
        <%= link_to "Delete", subscription_path(@subscription), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-delete" %>
      </div>
    </div>
